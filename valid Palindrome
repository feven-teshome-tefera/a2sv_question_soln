class Solution(object):
    def isPalindrome(self,s):
        n=[]
        g=s.lower()
        for i in g:
            if i.isalnum():
                n.append(i)
        word = ''.join(n)
        if word==word[::-1]:
            return True 
        else:
            return False  
